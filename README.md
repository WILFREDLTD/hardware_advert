# Hardware Campaign (minimal Next.js app)

This folder contains a minimal Next.js TypeScript single-page campaign for hardware store owners.

Run locally:

```bash
cd campaign
npm install
npm run dev
```

Open http://localhost:3000
# hardware_advert
